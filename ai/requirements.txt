flask              # Python 웹 서버 프레임워크. Node.js의 Express 같은 역할
flask-cors         # 다른 포트(프론트3003, 백엔드8008)에서 오는 요청 허용
python-dotenv      # .env 파일에서 OPENAI_API_KEY 같은 환경변수 읽어오기
openai             # GPT-4, Whisper, 임베딩 등 OpenAI API 호출
langchain          # AI 파이프라인 구성 도구. RAG 만들 때 핵심
langchain-openai   # langchain에서 OpenAI 모델 연결하는 브릿지
langchain-community # FAISS 등 커뮤니티 도구들 모음
faiss-cpu          # 벡터 유사도 검색 라이브러리. 코드 유사도 탐지에 사용
tiktoken           # 텍스트를 토큰으로 변환. GPT에 보내기 전 토큰 수 계산